{
  "scripts": {
    "local-test": "node ./helpers/test-runner.js",
    "test": "node ./helpers/test-runner.js",
    "install-deps": "npm install && cd ./site/ && npm install"
  },
  "private": true,
  "engines": {
    "node": ">=0.10.0"
  },
  "dependencies": {
    "gulp": "^4.0.2"
  },
  "devDependencies": {
    "cypress": "^7.3.0",
    "wait-port": "^0.2.9"
  }
}
